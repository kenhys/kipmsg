--- kipmsg-0.1.1/libtool.orig	Thu Jan 11 04:37:49 2007
+++ kipmsg-0.1.1/libtool	Thu Jan 11 04:34:48 2007
@@ -2318,6 +2318,9 @@
 	  continue
 	  ;;
 	esac # case $deplib
+	if test -z "$lib"; then
+		continue
+	fi
 	if test "$found" = yes || test -f "$lib"; then :
 	else
 	  $echo "$modename: cannot find the library \`$lib'" 1>&2
